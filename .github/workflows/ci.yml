name: CI - Tests + Build Docker
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installer Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Lancer les tests JUnit 5
        run: mvn --batch-mode test

  build-push:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Connexion a ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build et push de l'image Docker
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: |
            ghcr.io/sofiane-cheriette/tp-junit:latest
            ghcr.io/sofiane-cheriette/tp-junit:${{ github.sha }}

  deploy:
    needs: build-push
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3

      - name: Deploy to K8s (optional)
        env:
          KUBECONFIG_DATA: ${{ secrets.KUBECONFIG }}
        run: |
          if [ -z "$KUBECONFIG_DATA" ]; then
            echo "KUBECONFIG secret absent: deploy ignore (normal pour Minikube local)."
            exit 0
          fi

          echo "$KUBECONFIG_DATA" | base64 -d > kubeconfig

          if ! kubectl --kubeconfig=./kubeconfig cluster-info >/dev/null 2>&1; then
            echo "Cluster inaccessible depuis le runner GitHub: deploy ignore."
            exit 0
          fi

          kubectl apply -f k8s/ --kubeconfig=./kubeconfig
