apiVersion: apps/v1
kind: Deployment
metadata:
  name: boutique
  namespace: default
  labels:
    app: boutique
spec:
  replicas: 3
  selector:
    matchLabels:
      app: boutique
  template:
    metadata:
      labels:
        app: boutique
    spec:
      containers:
        - name: boutique
          image: ghcr.io/sofiane-cheriette/tp-junit:latest
          imagePullPolicy: Never
          command: ["java", "-cp", "app.jar", "fr.boutique.Application"]
          ports:
            - containerPort: 8080
          # L'application expose actuellement la route '/';
          # on utilise ce endpoint pour liveness/readiness.
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          envFrom:
            - configMapRef:
                name: boutique-config
            - secretRef:
                name: boutique-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
